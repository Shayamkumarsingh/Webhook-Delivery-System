apiVersion: apps/v1
kind: Deployment
metadata:
  name: rate-limit-service
  namespace: webhook-platform
  labels:
    app: rate-limit-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rate-limit-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: rate-limit-service
    spec:
      containers:
        - name: rate-limit-service
          image: your-dockerhub/rate-limit-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5009
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: webhook-platform-config
                  key: RATE_LIMIT_SERVICE_PORT
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: webhook-platform-config
                  key: REDIS_HOST
            - name: REDIS_PORT
              valueFrom:
                configMapKeyRef:
                  name: webhook-platform-config
                  key: REDIS_PORT
            - name: KAFKA_BROKER
              valueFrom:
                configMapKeyRef:
                  name: webhook-platform-config
                  key: KAFKA_BROKER
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: webhook-platform-config
                  key: NODE_ENV
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "300m"
          livenessProbe:
            httpGet:
              path: /health
              port: 5009
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 5009
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: rate-limit-service
  namespace: webhook-platform
  labels:
    app: rate-limit-service
spec:
  selector:
    app: rate-limit-service
  ports:
    - name: http
      port: 5009
      targetPort: 5009
  type: ClusterIP

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: rate-limit-service-hpa
  namespace: webhook-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: rate-limit-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 75
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 30
      policies:
        - type: Pods
          value: 2
          periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Pods
          value: 1
          periodSeconds: 120